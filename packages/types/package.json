{
  "name": "@prometheus-fe/types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --dts-only --minify",
    "dev": "tsup src/index.ts --format esm --dts --dts-only --minify --watch",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "devDependencies": {
    "@prometheus-fe/eslint-config": "workspace:*",
    "@prometheus-fe/typescript-config": "workspace:*",
    "eslint": "^9",
    "tsup": "^8.0.2",
    "typescript": "^5"
  },
  "exports": {
    ".": "./dist"
  }
}
